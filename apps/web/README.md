# Web (Next.js)

This folder will contain the Next.js app deployed to Vercel.

Planned responsibilities:

- Auth + UI
- Direct-to-Supabase Storage uploads (large PDFs)
- Job creation (enqueue parsing)
- Progress + results display (analysis + generated letters)
